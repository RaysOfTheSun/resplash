import { FullSizeOnClickDirective } from './full-size-on-click.directive';

describe('FullSizeOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new FullSizeOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
